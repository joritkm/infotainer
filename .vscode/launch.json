{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "App Launch",
            "cargo": {
                "args": [
                    "build",
                ]
            },
            "program": "${workspaceFolder}/app/target/${cargo:program}",
            "cwd": "${workspaceFolder}",
        },
    ]
}